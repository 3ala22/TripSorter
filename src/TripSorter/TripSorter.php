<?php

namespace TripSorter;

class TripSorter
{
    /**
     * Sort a set of unordered boarding passes.
     *
     * @param array $boardingCards
     * @return array
     */
    public function sort(array $boardingCards)
    {
        $sortedCards = [];

        return $sortedCards;
    }
}